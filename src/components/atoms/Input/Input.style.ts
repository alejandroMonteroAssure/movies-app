import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 40,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#444',
    paddingHorizontal: 12,
    color: '#fff',
    backgroundColor: '#222',
  },
});
