import { StyleSheet } from 'react-native';

export const MovieItemStyles = StyleSheet.create({
    card: {
        marginRight: 15,
        width: 120,
        alignItems: 'center',
    },
    poster: {
        height: 180,
        borderRadius: 10,
        marginBottom: 8,
    },
})